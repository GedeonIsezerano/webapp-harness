[project]
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.20,<5",
]

[dependency-groups]
dev = [
  "pytest>=8,<9",
]

[tool.pytest.ini_options]
testpaths = ["tests/harness"]
addopts = "-q"
