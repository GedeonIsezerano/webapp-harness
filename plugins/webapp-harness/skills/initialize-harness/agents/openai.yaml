interface:
  display_name: "Initialize GitHub Harness"
  short_description: "Configure issue-backed harness state"
  default_prompt: "Use $webapp-harness:initialize-harness to configure GitHub issue-backed harness state without adding repository state files."
