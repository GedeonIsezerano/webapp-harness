interface:
  display_name: "Run Backlog Cycle"
  short_description: "Run verified tasks sequentially to completion"
  default_prompt: "Use $webapp-harness:orchestrate-development-cycle to run the ready backlog sequentially until it is complete."
